@* @page "/produtos/novo"
@rendermode InteractiveServer

@using AppExemplo.Models;
@inject ProdutoDAO ProdutoDAO;
@inject NavigationManager Navigation;

<PageTitle>Produtos - Novo</PageTitle>

<h1 class="text-xl font-semibold text-gray-800 dark:text-gray-100 mb-6">Novo Produto</h1>

@if (erro != null)
{
    <div class="alert alert-danger">
        @erro
    </div>
}

<div class="row">
    <div class="col">
        <form>
            <div class="form-group mb-4">
                <label>Nome</label>
                <input @bind="_novoProduto.Nome" type="text" class="form-control" name="nome" placeholder="Informe o nome do produto" />
            </div>

            <div class="form-group mb-4">
                <label>Quantidade</label>
                <input @bind="_novoProduto.Quantidade" type="text" class="form-control" name="quantidade" placeholder="Informe a quantidade de produto" />
            </div>

            <div class="form-group mb-4">
                <label>Preço</label>
                <input @bind="_novoProduto.Preco" type="text" class="form-control" name="preco" placeholder="Informe a preco do produto" />
            </div>

            <div class="form-group mb-4">
                <label>Descrição</label>
                <textarea @bind="_novoProduto.Descricao" class="form-control" rows="5"></textarea>
            </div>

            <button @onclick="Salvar" class="btn btn-primary">Salvar</button>
        </form>
    </div>
</div>

@code {
    private string? erro = null;

    private Produto _novoProduto = new();

    private void Salvar()
    {
        ProdutoDAO.Inserir(_novoProduto);
    }
} *@
